{
  "name": "md-image-path-lsp",
  "version": "0.1.1",
  "description": "Markdown image path completion language server",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yuzukq/md-image-path-lsp.git",
    "directory": "server"
  },
  "bin": {
    "md-image-path-lsp": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=19.1.0"
  },
  "scripts": {
    "build": "tsc -p .",
    "watch": "tsc -p . --watch",
    "start": "node dist/index.js --stdio",
    "test": "node --test src/**/*.test.ts"
  },
  "dependencies": {
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.12",
    "globby": "^14.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "@types/node": "^24.0.0"
  }
}
